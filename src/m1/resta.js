/**
 * Return the difference betwen two numbers
 * @param {number} a first number
 * @param {number} b second number
 * @returns diff a-b
 */
const resta = (a,b)=>{
    return a-b;
};

export default resta;
